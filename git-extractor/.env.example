# GitHub
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_OWNER=your-org-or-user
GITHUB_REPOS=repo1,repo2           # comma-separated, leave empty for ALL repos

# MySQL / Aurora
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ai_governance
DB_USER=root
DB_PASSWORD=secret

# Extraction window (ISO-8601, optional — defaults to last 24h)
SINCE=
UNTIL=
